#playingfield(ng-controller="ZombiesViewCtrl as ctrl")
  .playingfield-row(ng-repeat="row in ctrl.view_tiles")
    zombie-tile(
      ng-repeat="view_tile in row track by $index"
      tile="view_tile"
      view="ctrl"
      coord="{{$index}}:{{$parent.$index}}"
    )
